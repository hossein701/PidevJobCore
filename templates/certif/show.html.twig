{% extends 'base.html.twig' %}

{% block title %}Certif{% endblock %}

{% block content %}
    <div class="vertical-space-85"></div>
    <div class="vertical-space-85"></div>
    <h1>Certif</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ certif.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ certif.nom }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ certif.date ? certif.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('certif_index') }}">back to list</a>

    <a href="{{ path('certif_edit', {'id': certif.id}) }}">edit</a>

    {{ include('certif/_delete_form.html.twig') }}
{% endblock %}
