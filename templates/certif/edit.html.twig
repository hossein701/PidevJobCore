{% extends 'base.html.twig' %}

{% block title %}Edit Certif{% endblock %}

{% block content %}
    <div class="vertical-space-85"></div>
    <div class="vertical-space-85"></div>
    <h1>Edit Certif</h1>

    {{ include('certif/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('certif_index') }}">back to list</a>

    {{ include('certif/_delete_form.html.twig') }}
{% endblock %}
